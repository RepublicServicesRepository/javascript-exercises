/**
 * Character Count
 *
 * Return an object where the key is a character in the
 * string and the value is the number of types the character
 * exists in the given string.
 *
 * Examples:
 * console.log(characterCount('mississippi'));
 * { 'm': 1, 'i': 4, 's': 4, 'p': 2 }
 */

function characterCount(string) {
  
}

module.exports = characterCount;
